# ========================
# HTTP → HTTPS
# ========================
server {
    listen 80;
    server_name api.hiki.io.vn;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    resolver 127.0.0.11 valid=30s;
    location / {
        return 301 https://$host$request_uri;
    }
}

# ========================
# HTTPS - API
# ========================
server {
    listen 443 ssl;
    server_name api.hiki.io.vn;

    ssl_certificate     /etc/letsencrypt/live/api.hiki.io.vn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.hiki.io.vn/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    resolver 127.0.0.11 valid=30s;
    location / {
        proxy_pass http://backend:8888;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# ========================
# HTTP → HTTPS - ADMIN
# ========================
server {
    listen 80;
    server_name admin.hiki.io.vn;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    resolver 127.0.0.11 valid=30s;
    location / {
        return 301 https://$host$request_uri;
    }
}

# ========================
# HTTPS - ADMIN
# ========================
server {
    listen 443 ssl;
    server_name admin.hiki.io.vn;

    ssl_certificate     /etc/letsencrypt/live/admin.hiki.io.vn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/admin.hiki.io.vn/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    resolver 127.0.0.11 valid=30s;
    location / {
        proxy_pass http://admin:80;   # hoặc port admin container nếu khác
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
